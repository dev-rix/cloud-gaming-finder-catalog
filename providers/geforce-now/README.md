# GeForce NOW provider

This provider publishes the normalized GeForce NOW PC catalog.

The generated file is `catalog/providers/geforce-now.json`. Keep provider-specific source assumptions and transformations in this directory; do not put them into the shared catalog schema.
